<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EVA1"
        tools:targetApi="31">

        <!-- MainActivity debe ser la actividad inicial -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.EVA1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WelcomeActivity se lanzará después del login -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true"
            android:label="@string/title_activity_welcome"
            android:theme="@style/Theme.EVA1">

        </activity>

        <!-- RegisterUsersActivity solo se lanzará desde el botón de registrar nuevo usuario -->
        <activity
            android:name=".RegisterUsersActivity"
            android:exported="true"
            android:label="@string/title_activity_register_users"
            android:theme="@style/Theme.EVA1">
        </activity>
    </application>

</manifest>
